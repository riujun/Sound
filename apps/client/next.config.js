/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'asset.cloudinary.com',
      'res.cloudinary.com',
      'asset.cloudinary.com',
      'via.placeholder.com',
    ],
  },
};

module.exports = nextConfig;
